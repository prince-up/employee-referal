# PayrollPro

PayrollPro is a React and Supabase HR and payroll management application.

## Local development

```bash
npm install
copy .env.example .env
npm run dev
```

Configure the Supabase URL and anon key in `.env`, then run `supabase/migrations/001_schema.sql` in the Supabase SQL editor before using the application.
